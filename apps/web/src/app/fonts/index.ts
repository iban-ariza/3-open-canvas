import localFont from 'next/font/local';

export const arboriaBold = localFont({
    src: './Arboria-Bold.woff2',
    variable: '--font-arboria-bold',
    display: 'swap',
});

export const arboriaBook = localFont({
    src: './Arboria-Book.woff2',
    variable: '--font-arboria-book',
    display: 'swap',
});
